jQuery(document).ready(
    function(){
        /*jQuery('div').each(
            function(){
                var top = jQuery(this).height() > (window.innerHeight / 2)  
                    ? window.innerHeight - window.innerHeight / 3 
                    : (window.innerHeight - jQuery(this).height() - 2*jQuery(this).height() * Math.random());
                jQuery(this).animate(
                    {
                        position: 'absolute',
                        top: top,
                        opacity: 1
                    }, 700);
            });
        var p = $('p');
        var text = p.text().split(' ');
        
        for( var i = 1, len = text.length; i < len; i=i+2 ) {
            
            text[i] = '<span>' + text[i] + '</span>';
        }
        p.html(text.join(' '));
    
    
        jQuery('span').each(
            function(){
                jQuery(this).animate(
                    {
                        top: window.innerHeight,
                        opacity: 1
                    }, 3000);
            });*/
        
    });